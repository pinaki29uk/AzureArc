apiVersion: v1
kind: Service
metadata:
  name: {{ include "AzureArc.fullname" . }}
  labels:
    {{- include "AzureArc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "AzureArc.selectorLabels" . | nindent 4 }}
