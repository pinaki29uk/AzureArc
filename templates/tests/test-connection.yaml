apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "AzureArc.fullname" . }}-test-connection"
  labels:
    {{- include "AzureArc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "AzureArc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
